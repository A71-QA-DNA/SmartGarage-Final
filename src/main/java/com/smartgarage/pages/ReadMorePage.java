package com.smartgarage.pages;

import com.testframework.core.BaseWebPage;
import org.openqa.selenium.WebDriver;

public class ReadMorePage extends BaseWebPage {
    protected ReadMorePage(WebDriver webDriver) {
        super(webDriver, "about");
    }
}
