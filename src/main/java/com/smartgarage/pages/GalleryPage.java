package com.smartgarage.pages;

import com.testframework.core.BaseWebPage;
import org.openqa.selenium.WebDriver;

public class GalleryPage extends BaseWebPage {
    protected GalleryPage(WebDriver webDriver) {
        super(webDriver, "gallery");
    }
}
